import 'package:flutter/material.dart';

class ReadLsmScreen extends StatelessWidget {
  const ReadLsmScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}